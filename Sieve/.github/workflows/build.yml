# Builds and tests Sieve on Windows, Linux and macOS on every push and pull request.
#
# GitHub only runs workflows from the repository root's .github/workflows folder. If this project
# lives in a subfolder of the repository (for example Potentia/Sieve), move this file to
# <repository root>/.github/workflows/build.yml and set PROJECT_DIR below to that subfolder.
name: build

on: [push, pull_request]

env:
  PROJECT_DIR: Sieve

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Unit and conformance tests
        run: ctest --test-dir build -C Release --output-on-failure

      - name: Same addresses on every platform
        run: |
          exe=build/sieve
          [ -f build/Release/sieve.exe ] && exe=build/Release/sieve.exe
          $exe version
          got=$($exe warp --length 32 --mode scrambled "It was the best of times" | awk '/scrambled/ {print $2}')
          echo "scrambled address: $got"
          test "$got" = "007b30165818bf0311497600aa52996f9395e27"
          test "$($exe read --length 32 --mode scrambled "$got" | sed 's/ *$//')" = "it was the best of times"
          $exe dicts
          ! $exe dicts | grep -q "MISMATCH\|MISSING"
          $exe sift --lengths 1-8 --brute-max 4 --pruned-max 5 > /dev/null
          $exe models
          ! $exe models | grep -q "MISMATCH\|MISSING\|UNREADABLE"
          got=$($exe warp --length 32 --mode guided "It was the best of times" | awk '$1 == "guided" {print $2}')
          echo "guided address: $got"
          test "$got" = "90922c700685e28"
          test "$($exe read --length 32 --mode guided "$got" | sed 's/ *$//')" = "it was the best of times"

      - name: Filters judge, count and rank the same on every platform
        run: |
          exe=build/sieve
          [ -f build/Release/sieve.exe ] && exe=build/Release/sieve.exe
          printf '[text]\nmode = compact\nfilters = words-v2\n' > words2.ini
          printf '[text]\nmode = compact\nfilters = words-v2, max-run-v1\n' > mixed.ini
          $exe filters --filters words2.ini | tr -d '\r' | grep -q "^survivors    190011992984255955985337560 "
          $exe filters --filters mixed.ini | tr -d '\r' | grep -q "words-v2 can rank, but not together with max-run-v1"
          out=$($exe check --length 32 --filters words2.ini "It was the best of times" | tr -d '\r')
          echo "$out"
          echo "$out" | grep -q "words-v1 *FAIL"
          echo "$out" | grep -q "words-v2 *pass"
          k=$(echo "$out" | awk '/survivor number/ {print $(NF-2)}')
          test "$k" = "100485593897630338697104005"
          test "$($exe read --length 32 --mode positional --filters words2.ini --survivor "$k" 2>/dev/null | tr -d '\r' | sed 's/ *$//')" = "it was the best of times"
          test "$($exe read --length 32 --mode positional --filters words2.ini --survivor 1000000 2>/dev/null | tr -d '\r' | sed 's/ *$//')" = " a a a a a a a a a a a aah seize"
          # Compact in every ordering: warp gives the compact addresses, read takes them back.
          out=$($exe warp --length 32 --filters words2.ini --compact "It was the best of times" 2>/dev/null | tr -d '\r')
          echo "$out"
          for m in positional scrambled guided; do
            addr=$(echo "$out" | awk -v m=$m '/compact/ {seen = 1; next} seen && $1 == m {print $2; exit}')
            echo "compact $m: $addr"
            test "$($exe read --length 32 --mode $m --filters words2.ini --compact "$addr" 2>/dev/null | tr -d '\r' | sed 's/ *$//')" = "it was the best of times"
          done
          echo "$out" | grep -q "scrambled   1db41d1685abd564b15405"
          echo "$out" | grep -q "guided      9116ae0428ea154  (58 bits"
          # The other lines: melodies in a key, black-and-white pictures of low entropy.
          printf '[audio]\nmode = compact\nfilters = key-v1\n[audio.key-v1]\ntonic = A\nscale = minor-pentatonic\n[image]\nmode = compact\nfilters = symbol-entropy-v1\n[image.symbol-entropy-v1]\nmax_millibits = 500\n' > lines.ini
          $exe filters --line audio --filters lines.ini | tr -d '\r' | grep -q "^survivors    794071845499378503449051136 "
          $exe filters --line image --filters lines.ini | tr -d '\r' | grep -q "^survivors    322091425582872 "
          printf '[image]\nmode = compact\nfilters = neighbour-agreement-v1\n' > agree.ini
          $exe filters --line image --filters agree.ini | tr -d '\r' | grep -q "^survivors    6053792982126620565936264884 "
          $exe browse --line image --filters agree.ini --compact --count 2 --seed 4 > /dev/null
          $exe browse --line audio --filters lines.ini --compact --count 2 --seed 4 > /dev/null

      - name: Hallway renders, and its doors keep the corridor position (Linux, no display)
        if: runner.os == 'Linux'
        env:
          SDL_VIDEO_DRIVER: offscreen
          SDL_RENDER_DRIVER: software
        run: |
          H=build/hallway
          W="it was the best of times"
          book() { grep -o 'first book [0-9a-f.]*' | tail -1; }
          $H --screenshot hallway.png --size 640x360
          test -s hallway.png
          here=$($H --warp "$W" --walk "0,0" --screenshot a.png | book)
          next=$($H --warp "$W" --tile 1 --walk "0,0" --screenshot b.png | book)
          # Through a door and straight back: exactly where you were.
          out=$($H --warp "$W" --pose 0,7,-90,0 --walk "-2.3,0;2.5,0" --screenshot door.png)
          echo "$out"
          echo "$out" | head -1 | grep -q "through the door into the IMAGE line"
          test "$(echo "$out" | tail -2 | book)" = "$here"
          # Through a door, one tile along, back through the door there: one tile along in text.
          out=$($H --warp "$W" --pose 0,7,-90,0 --walk "-2.3,0;-0.8,0;0,8;1.5,0" --screenshot loop.png)
          echo "$out"
          echo "$out" | tail -2 | head -1 | grep -q "PAGES line"
          test "$(echo "$out" | tail -2 | book)" = "$next"
          # Guided order: M cycles to it and - zooms out; all four doors lead back to the same book.
          $H --warp "$W" --press "M,M,-" --screenshot keys.png | tail -2 | head -1 | grep -q "PAGES line, guided zoom 57"
          start=$($H --mode guided --warp "$W" --zoom 20 --walk "0,0" --screenshot start.png | book)
          out=$($H --mode guided --warp "$W" --zoom 20 --pose 0,7,-90,0 --walk "-2.3,0;-4.2,0;-4.2,0;-4.2,0" --screenshot guided.png)
          echo "$out"
          echo "$out" | grep -q "VIDEO line"
          echo "$out" | tail -2 | head -1 | grep -q "PAGES line, guided"
          test "$(echo "$out" | tail -2 | book)" = "$start"
          # Loops: a 2-character line is 729 units = 6 tiles, so tile 6 starts the second copy,
          # and the CLI reads the same corridor places the hallway shows.
          $H --length 2 --goto @6 --walk "0,0" --screenshot loop6.png | grep -q "first book 000 "
          test "$(build/sieve read --length 2 --mode positional --at 6:0 2>/dev/null)" = "  "
          test "$(build/sieve read --length 2 --mode positional --at -1:127 2>/dev/null)" = "(empty: padding at the end of a loop)"
          build/sieve info --length 2 | grep -q "39 empty slots"
          build/sieve info --line image | grep -q "fills whole tiles exactly"
          # The setup menu renders at ordinary and enormous sizes, and scripted keys reach it.
          $H --menu --screenshot menu.png | grep -q "saved menu.png"
          $H --menu --press "Down,Down,Down,Right,PageUp" --screenshot menu2.png | grep -q "saved menu2.png"
          $H --menu --length 4000000000 --video-width 100000 --video-frames 100000 --screenshot menu3.png | grep -q "saved menu3.png"
          test -s menu.png && test -s menu2.png && test -s menu3.png
          # Filter modes: compact shelves only survivors (warped text lands on a shelf, not in hand),
          # an unrankable stack falls back to hide, and the filter overlay renders.
          printf '[text]\nmode = compact\nfilters = words-v2\n' > words2.ini
          printf '[text]\nmode = compact\nfilters = words-v2, max-run-v1\n' > mixed.ini
          printf '[text]\nmode = mark\nfilters = words-v1\n' > mark.ini
          printf '[audio]\nmode = compact\nfilters = key-v1\n[audio.key-v1]\ntonic = A\nscale = minor-pentatonic\n[image]\nmode = compact\nfilters = symbol-entropy-v1\n[image.symbol-entropy-v1]\nmax_millibits = 500\n' > lines.ini
          out=$($H --filters words2.ini --warp "$W" --walk "0,0" --screenshot compact.png)
          echo "$out"
          echo "$out" | grep -q "filters: 1 compact (190011...7560 (27 digits) survivors), tile 785043...1125 (24 digits)"
          $H --filters mixed.ini --walk "0,0" --screenshot mixed.png | grep -q "filters: 2 hide (compact: words-v2 can rank, but not together with max-run-v1)"
          $H --filters mark.ini --length 3 --walk "0,0" --screenshot mark.png | grep -q "filters: 1 mark"
          $H --menu --filters words2.ini --press "F" --screenshot overlay.png | grep -q "saved overlay.png"
          # Compact in the scrambled and guided orderings, and on the image and audio lines.
          $H --filters words2.ini --mode scrambled --warp "$W" --walk "0,0" --screenshot cs.png | head -1 | grep -q "PAGES line, scrambled, filters: 1 compact"
          $H --filters words2.ini --mode guided --warp "$W" --walk "0,0" --screenshot cg.png | head -1 | grep -q "PAGES line, guided zoom 58, filters: 1 compact"
          $H --filters lines.ini --line image --mode scrambled --walk "0,0" --screenshot ci.png | head -1 | grep -q "IMAGE line, scrambled, filters: 1 compact (322091425582872 survivors)"
          $H --filters lines.ini --line audio --walk "0,0" --screenshot ca.png | head -1 | grep -q "AUDIO line, positional, filters: 1 compact"
          test -s compact.png && test -s mixed.png && test -s mark.png && test -s overlay.png

      - name: Reference oracle reproduces the committed vectors
        run: |
          cd reference
          python3 sieve_ref.py vectors       | diff - ../tests/vectors_v1.tsv
          python3 sieve_ref.py digit-vectors | diff - ../tests/vectors_digits_v1.tsv
          python3 sieve_ref.py canon-vectors | diff - ../tests/vectors_canon.tsv
          python3 sieve_ref.py image-vectors | diff - ../tests/vectors_image_v1.tsv
          python3 sieve_ref.py guided-vectors | diff - ../tests/vectors_guided_v1.tsv
          python3 sieve_ref.py filter-vectors | diff - ../tests/vectors_filters_v1.tsv
          python3 sieve_ref.py compact-vectors | diff - ../tests/vectors_compact_v1.tsv

      - name: The pinned model rebuilds byte for byte from its corpus (Linux)
        if: runner.os == 'Linux'
        run: |
          python3 tools/fetch_corpus.py
          pinned=$(awk -F'\t' '$1 == "gutenberg-lower27-o5" {print $5}' data/models/models.tsv)
          build/sieve train --out rebuilt.model
          test "$(sha256sum rebuilt.model | cut -d' ' -f1)" = "$pinned"
          (cd reference && python3 sieve_ref.py model-build --out ../oracle.model)
          test "$(sha256sum oracle.model | cut -d' ' -f1)" = "$pinned"
          build/sieve measure
